Add one sub-folder per item inside this directory.
Place the listing photos for a single item in that sub-folder.
If search_image_name is blank in config.json, the first supported image alphabetically is used for the eBay image search.
